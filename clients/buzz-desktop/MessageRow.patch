diff --git a/desktop/src/features/messages/ui/MessageRow.tsx b/desktop/src/features/messages/ui/MessageRow.tsx
index facf562..d68eb52 100644
--- a/desktop/src/features/messages/ui/MessageRow.tsx
+++ b/desktop/src/features/messages/ui/MessageRow.tsx
@@ -1,3 +1,5 @@
+import { KnowledgeCard } from "./KnowledgeCard";
+import { parseKnowledgeCard } from "./knowledgeCardModel";
 import * as React from "react";
 import { AlertTriangle } from "lucide-react";
 import {
@@ -368,6 +370,8 @@ export const MessageRow = React.memo(
       message.tags?.find((tag) => tag[0] === name)?.[1];
 
     const renderBody = () => {
+      const knowledgeCard = parseKnowledgeCard(message, channelId, import.meta.env.VITE_BUZZ_KNOWLEDGE_PUBKEY);
+      if (knowledgeCard) return <KnowledgeCard card={knowledgeCard} messageId={message.id} />;
       switch (message.kind) {
         case KIND_STREAM_MESSAGE_DIFF:
           return (
