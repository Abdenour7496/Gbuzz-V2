# Opt-in graph extension. Append to the deployment's existing Compose files.
# The core keeps durable projection records while this extension is disabled.
services:
  falkordb:
    profiles: !reset []
  graphiti-mcp:
    profiles: !reset []
  graphiti-projector:
    profiles: !reset []
  graphiti-model-init:
    profiles: !reset []
