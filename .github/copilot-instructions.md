# MCP implementation

The MCP server uses the Python SDK documented at https://modelcontextprotocol.io.
Keep `mcp-postgres-gcor` transport-compatible with the SSE endpoint declared in `.vscode/mcp.json`.